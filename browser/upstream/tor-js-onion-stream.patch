diff --git a/crates/tor-js-wasm/Cargo.toml b/crates/tor-js-wasm/Cargo.toml
--- a/crates/tor-js-wasm/Cargo.toml
+++ b/crates/tor-js-wasm/Cargo.toml
@@ -23,6 +23,8 @@
 ]}
 tor-rtcompat = { git = "https://github.com/voltrevo/arti", rev = "2be5b51e895a687d1e7fc3ce1c01896730bcb9d3", default-features = false }
 tor-persist = { git = "https://github.com/voltrevo/arti", rev = "2be5b51e895a687d1e7fc3ce1c01896730bcb9d3", default-features = false }
+tor-hscrypto = { git = "https://github.com/voltrevo/arti", rev = "2be5b51e895a687d1e7fc3ce1c01896730bcb9d3", default-features = false }
+safelog = { git = "https://github.com/voltrevo/arti", rev = "2be5b51e895a687d1e7fc3ce1c01896730bcb9d3", default-features = false }
 tor-dirmgr = { git = "https://github.com/voltrevo/arti", rev = "2be5b51e895a687d1e7fc3ce1c01896730bcb9d3", default-features = false, features = ["zstd-wasm"] }
 tor-llcrypto = { git = "https://github.com/voltrevo/arti", rev = "2be5b51e895a687d1e7fc3ce1c01896730bcb9d3" }
 tor-netdoc = { git = "https://github.com/voltrevo/arti", rev = "2be5b51e895a687d1e7fc3ce1c01896730bcb9d3" }
diff --git a/crates/tor-js-wasm/src/lib.rs b/crates/tor-js-wasm/src/lib.rs
--- a/crates/tor-js-wasm/src/lib.rs
+++ b/crates/tor-js-wasm/src/lib.rs
@@ -37,6 +37,8 @@
 mod fetch;
 mod runtime;
 mod storage;
+mod onion_stream;
+pub use onion_stream::OnionStream;
 
 pub use storage::{JsStorage, JsStorageInterface, CachedJsStorage};
 
@@ -292,6 +294,7 @@
 pub struct TorClient {
     inner: Option<Arc<ArtiTorClient<WasmRuntime>>>,
     tls_config: Arc<futures_rustls::rustls::ClientConfig>,
+    onion_streams: std::rc::Rc<onion_stream::OnionStreamRegistry>,
 }
 
 #[wasm_bindgen]
@@ -376,6 +379,7 @@
     #[wasm_bindgen(js_name = close)]
     pub fn close(&mut self) -> js_sys::Promise {
         self.inner = None;
+        self.onion_streams.close();
         wasm_bindgen_futures::future_to_promise(async {
             info!("TorClient closed");
             Ok(JsValue::undefined())
@@ -448,6 +452,7 @@
     Ok(TorClient {
         inner: Some(tor_client),
         tls_config: make_tls_config(),
+        onion_streams: std::rc::Rc::new(onion_stream::OnionStreamRegistry::default()),
     })
 }
 
diff --git a/src/TorClient.ts b/src/TorClient.ts
--- a/src/TorClient.ts
+++ b/src/TorClient.ts
@@ -30,6 +30,20 @@
     await ensureWasmInitialized();
     return typeof WasmTorClient.onionClientSupported === 'function'
       && WasmTorClient.onionClientSupported();
+  }
+
+  static async onionStreamSupported(): Promise<boolean> {
+    await ensureWasmInitialized();
+    return typeof WasmTorClient.onionStreamSupported === 'function'
+      && WasmTorClient.onionStreamSupported();
+  }
+
+  async connectOnion(host: string, port: number, deadlineMs: number) {
+    if (this.closed) throw new Error('tor-js:OnionTransport');
+    const client = await this.clientPromise;
+    await this.ready();
+    if (this.closed) throw new Error('tor-js:OnionTransport');
+    return client.connectOnion(host, port, deadlineMs);
   }
 
   constructor(options: TorClientOptions = {}) {
