diff --git a/crates/tor-persist/src/lib.rs b/crates/tor-persist/src/lib.rs
--- a/crates/tor-persist/src/lib.rs
+++ b/crates/tor-persist/src/lib.rs
@@ -63,7 +63,12 @@
 mod testing;
 
 #[cfg(feature = "state-dir")]
+#[cfg_attr(target_arch = "wasm32", path = "state_dir_wasm.rs")]
 pub mod state_dir;
+
+#[cfg(all(test, feature = "state-dir", not(target_arch = "wasm32")))]
+#[path = "state_dir_wasm.rs"]
+mod state_dir_wasm_tests;
 
 use serde::{Deserialize, Serialize, de::DeserializeOwned};
 use std::sync::Arc;
diff --git a/crates/arti-client/src/client.rs b/crates/arti-client/src/client.rs
--- a/crates/arti-client/src/client.rs
+++ b/crates/arti-client/src/client.rs
@@ -579,9 +579,14 @@
             action: "create onion service",
         })?;
 
-        let (state_dir, mistrust) = config.state_dir()?;
-        let state_dir =
-            self::StateDirectory::new(state_dir, mistrust).map_err(ErrorDetail::StateAccess)?;
+        #[cfg(not(target_arch = "wasm32"))]
+        let state_dir = {
+            let (state_dir, mistrust) = config.state_dir()?;
+            self::StateDirectory::new(state_dir, mistrust).map_err(ErrorDetail::StateAccess)?
+        };
+        #[cfg(target_arch = "wasm32")]
+        let state_dir = self::StateDirectory::new_ephemeral();
+
 
         Ok(tor_hsservice::OnionService::builder()
             .config(svc_config)
@@ -1005,9 +1010,11 @@
 
         #[cfg(not(target_arch = "wasm32"))]
         let (state_dir, mistrust) = config.state_dir()?;
-        #[cfg(feature = "onion-service-service")]
+        #[cfg(all(feature = "onion-service-service", not(target_arch = "wasm32")))]
         let state_directory =
             StateDirectory::new(&state_dir, mistrust).map_err(ErrorDetail::StateAccess)?;
+        #[cfg(all(feature = "onion-service-service", target_arch = "wasm32"))]
+        let state_directory = StateDirectory::new_ephemeral();
 
         let dormant = DormantMode::Normal;
 
diff --git a/crates/tor-hsservice/src/internal_prelude.rs b/crates/tor-hsservice/src/internal_prelude.rs
--- a/crates/tor-hsservice/src/internal_prelude.rs
+++ b/crates/tor-hsservice/src/internal_prelude.rs
@@ -101,9 +101,7 @@
     tor_netdoc::NetdocBuilder,
     tor_netdoc::doc::hsdesc::{HsDescBuilder, create_desc_sign_key_cert},
     tor_persist::slug::Slug,
-    tor_persist::state_dir::{
-        ContainsInstanceStateGuard as _, InstanceRawSubdir, LockFileGuard, StateDirectory,
-    },
+    tor_persist::state_dir::StateDirectory,
     tor_proto::MetaCellDisposition,
     tor_proto::client::stream::DataStream,
     tor_rtcompat::SleepProvider,
@@ -112,6 +110,11 @@
 };
 
 //---------- names from this crate ----------
+
+#[cfg(not(target_arch = "wasm32"))]
+pub(crate) use tor_persist::state_dir::{
+    ContainsInstanceStateGuard as _, InstanceRawSubdir, LockFileGuard,
+};
 
 pub(crate) use {
     crate::OnionServiceConfig,
diff --git a/crates/tor-hsservice/src/lib.rs b/crates/tor-hsservice/src/lib.rs
--- a/crates/tor-hsservice/src/lib.rs
+++ b/crates/tor-hsservice/src/lib.rs
@@ -111,6 +111,9 @@
 use pow::{NewPowManager, PowManager};
 pub use publish::UploadError as DescUploadError;
 pub use req::{RendRequest, StreamRequest};
+
+#[cfg(all(target_arch = "wasm32", feature = "hs-pow-full"))]
+compile_error!("browser ephemeral service requires a port of the PoW replay store before hs-pow-full can be enabled");
 pub use tor_hscrypto::pk::HsId;
 use tor_keymgr::KeystoreEntry;
 pub use tor_persist::hsnickname::{HsNickname, InvalidNickname};
@@ -323,9 +326,12 @@
         let pow_manager_storage_handle = state_handle
             .storage_handle("pow_manager")
             .map_err(StartupError::StateDirectoryInaccessible)?;
+        #[cfg(not(target_arch = "wasm32"))]
         let pow_nonce_dir = state_handle
             .raw_subdir("pow_nonces")
             .map_err(StartupError::StateDirectoryInaccessible)?;
+        #[cfg(target_arch = "wasm32")]
+        let pow_nonce_dir = ();
         let NewPowManager {
             pow_manager,
             rend_req_tx,
diff --git a/crates/tor-hsservice/src/pow/v1_stub.rs b/crates/tor-hsservice/src/pow/v1_stub.rs
--- a/crates/tor-hsservice/src/pow/v1_stub.rs
+++ b/crates/tor-hsservice/src/pow/v1_stub.rs
@@ -10,9 +10,14 @@
 use tor_netdir::NetDirProvider;
 use tor_persist::{
     hsnickname::HsNickname,
-    state_dir::{InstanceRawSubdir, StorageHandle},
+    state_dir::StorageHandle,
 };
 use tor_rtcompat::Runtime;
+
+#[cfg(not(target_arch = "wasm32"))]
+use tor_persist::state_dir::InstanceRawSubdir;
+#[cfg(target_arch = "wasm32")]
+type InstanceRawSubdir = ();
 
 use crate::{OnionServiceConfig, RendRequest, StartupError, status::StatusSender};
 
diff --git a/crates/tor-hsservice/src/ipt_mgr.rs b/crates/tor-hsservice/src/ipt_mgr.rs
--- a/crates/tor-hsservice/src/ipt_mgr.rs
+++ b/crates/tor-hsservice/src/ipt_mgr.rs
@@ -78,6 +78,7 @@
     ///
     /// Files are named after the (bare) IptLocalId
     #[educe(Debug(ignore))]
+    #[cfg(not(target_arch = "wasm32"))]
     replay_log_dir: tor_persist::state_dir::InstanceRawSubdir,
 
     /// A sender for updating the status of the onion service.
@@ -388,6 +389,15 @@
         _: PromiseLastDescriptorExpiryNoneIsGood,
     ) -> Result<Ipt, CreateIptError> {
         let mut rng = tor_llcrypto::rng::CautiousRng;
+
+        // Browser runtime state and keys are never independently restored.
+        // A reconstructed introduction must not receive a new empty replay log.
+        #[cfg(target_arch = "wasm32")]
+        if expect_existing_keys.is_some() {
+            return Err(CreateIptError::Fatal(
+                internal!("ephemeral browser introduction cannot restore prior keys").into(),
+            ));
+        }
 
         /// Load (from disk) or generate an IPT key with role IptKeyRole::$role
         ///
@@ -460,7 +470,10 @@
         };
 
         // TODO #1186 Support ephemeral services (without persistent replay log)
+        #[cfg(not(target_arch = "wasm32"))]
         let replay_log = IptReplayLog::new_logged(&imm.replay_log_dir, &lid)?;
+        #[cfg(target_arch = "wasm32")]
+        let replay_log = IptReplayLog::new_ephemeral();
 
         let params = IptParameters {
             replay_log,
@@ -625,6 +638,7 @@
             .storage_handle("ipts")
             .map_err(StartupError::StateDirectoryInaccessible)?;
 
+        #[cfg(not(target_arch = "wasm32"))]
         let replay_log_dir = state_handle
             .raw_subdir("iptreplay")
             .map_err(StartupError::StateDirectoryInaccessible)?;
@@ -636,6 +650,7 @@
             status_send,
             output_rend_reqs,
             keymgr,
+            #[cfg(not(target_arch = "wasm32"))]
             replay_log_dir,
             status_tx,
         };
@@ -1359,6 +1374,8 @@
         }
 
         // IPT replay logs
+        #[cfg(not(target_arch = "wasm32"))]
+        {
 
         let handle_rl_err = |operation, path: &Path| {
             let path = path.to_owned();
@@ -1394,6 +1411,7 @@
             // Not known, remove it
             let path = ent.path();
             fs::remove_file(&path).map_err(handle_rl_err("remove", &path))?;
+        }
         }
 
         Ok(())
diff --git a/crates/tor-hsservice/src/replay.rs b/crates/tor-hsservice/src/replay.rs
--- a/crates/tor-hsservice/src/replay.rs
+++ b/crates/tor-hsservice/src/replay.rs
@@ -35,6 +35,7 @@
     /// Persistent state file etc., if we're persistent
     ///
     /// If is is `None`, this RelayLog is ephemeral.
+    #[cfg(not(target_arch = "wasm32"))]
     file: Option<PersistFile>,
     /// [`PhantomData`] so rustc doesn't complain about the unused type param.
     ///
@@ -92,6 +93,7 @@
 ///
 /// Stored as `ReplayLog.file`.
 #[derive(Debug)]
+#[cfg(not(target_arch = "wasm32"))]
 pub(crate) struct PersistFile {
     /// A file logging fingerprints of the messages we have seen.
     file: BufWriter<File>,
@@ -120,6 +122,7 @@
     pub(crate) fn new_ephemeral() -> Self {
         Self {
             seen: data::Filter::new(),
+            #[cfg(not(target_arch = "wasm32"))]
             file: None,
             replay_log_type: PhantomData,
         }
@@ -138,6 +141,7 @@
     /// It is the caller's responsibility to make sure that there are never two
     /// `ReplayLogs` open at once for the same path, or for two paths that
     /// resolve to the same file.
+    #[cfg(not(target_arch = "wasm32"))]
     pub(crate) fn new_logged(
         dir: &InstanceRawSubdir,
         name: &T::Name,
@@ -153,6 +157,7 @@
     }
 
     /// Inner function for `new_logged`, with reified arguments and raw error type
+    #[cfg(not(target_arch = "wasm32"))]
     fn new_logged_inner(path: impl AsRef<Path>, lock: Arc<LockFileGuard>) -> io::Result<Self> {
         let mut file = {
             let mut options = OpenOptions::new();
@@ -218,6 +223,7 @@
     ///
     /// `current_len` should have come from `file.metadata()`.
     // If the file's length is not an even multiple of MESSAGE_LEN after the MAGIC, truncate it.
+    #[cfg(not(target_arch = "wasm32"))]
     fn truncate_to_multiple(file: &mut File, current_len: u64) -> io::Result<()> {
         let excess = (current_len - T::MAGIC.len() as u64) % (OUTPUT_LEN as u64);
         if excess != 0 {
@@ -236,6 +242,7 @@
     pub(crate) fn check_for_replay(&mut self, message: &T::Message) -> Result<(), ReplayError> {
         let h = T::transform_message(message);
         self.seen.test_and_add(&h)?;
+        #[cfg(not(target_arch = "wasm32"))]
         if let Some(f) = self.file.as_mut() {
             (|| {
                 // If write_all fails, it might have written part of the data;
@@ -280,6 +287,7 @@
     /// Flush any buffered data to disk.
     #[allow(dead_code)] // TODO #1208
     pub(crate) fn flush(&mut self) -> Result<(), io::Error> {
+        #[cfg(not(target_arch = "wasm32"))]
         if let Some(f) = self.file.as_mut() {
             f.file.flush()?;
         }
